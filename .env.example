# Optional explicit override. All builds default to this public coordinator;
# deterministic local QA replaces it with its suite-owned coordinator URL.
EXPO_PUBLIC_CRAYS_COORDINATOR_URL=https://coordinator.nuts.cash
